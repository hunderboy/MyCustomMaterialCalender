<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.co.everex.mycustommaterialcalender">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TimeCalculateActivity"></activity>
        <activity android:name=".MaterialInFragmentActivity" />
        <activity android:name=".CosmoCalenderActivity" />
        <activity android:name=".MaterialCalenderActivity" /> <!-- android:theme="@style/Theme.MyCustomMaterialCalender" -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CustomCalenderActivity" />
    </application>

</manifest>